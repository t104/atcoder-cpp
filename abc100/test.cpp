#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); ++i)
using namespace std;
using ll = long long;
long long N, M, a[100009][3], maxn = -(1LL << 2);

int main() {
    cout << maxn << endl;
    return 0;
}