#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); ++i)
using namespace std;
using ll = long long;

int main() {
    double r;
    cin >> r;
    cout << fixed << setprecision(8);
    cout << 2*r*M_PI << endl;
    return 0;
}